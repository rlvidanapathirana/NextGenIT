// Full test

const Gpt_Client = require('gpt4free-client');

async function main() {
  const completion = new Gpt_Client.Completion();

  // streaming response
  await completion.create({
    model: "gpt-4-0613",
    temperature: 0.7,
    system_prompt: "You are ChatGPT, big trained AI model", // system_prompt
    messages: [{ role: 'user', content: 'hello world' }], // messages
    stream: true
  });

  for await (const chunk of completion) {
    console.log(chunk);
  }

  // non-stream response
  var response = await completion.create({
    model: "gpt-4-0613",
    temperature: 0.7,
    system_prompt: "You are ChatGPT, big trained AI model", // system_prompt
    messages: [{ role: 'user', content: 'hello world' }], // messages
    stream: false
  });

  console.log(response);
}

main()